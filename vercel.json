{
  "version": 2,
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/next"
    }
  ],
  "env": {
    "NEXT_PUBLIC_INSTA_WP_API_KEY": "${NEXT_PUBLIC_INSTA_WP_API_KEY}",
    "INSTA_WP_API_KEY": "${INSTA_WP_API_KEY}",
    "NEXT_PUBLIC_API_URL": "${NEXT_PUBLIC_API_URL}",
    "NEXT_PUBLIC_DEEPSEEK_API_KEY": "${NEXT_PUBLIC_DEEPSEEK_API_KEY}",
    "NEXT_PUBLIC_QWEN_API_KEY": "${NEXT_PUBLIC_QWEN_API_KEY}",
    "NEXT_PUBLIC_OPENAI_API_KEY": "${NEXT_PUBLIC_OPENAI_API_KEY}"
  }
}

